<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blog Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 text-gray-800">
    <header class="bg-blue-600 text-white py-4 shadow-md">
      <div class="container mx-auto flex justify-between items-center px-4">
        <h1 class="text-2xl font-bold">Blog Home</h1>
        <a
          href="/add-post"
          class="bg-white text-blue-600 px-4 py-2 rounded shadow hover:bg-gray-200"
          >Add New Post</a
        >
      </div>
    </header>
    <main
      class="container mx-auto px-8 py-4 mt-6 grid lg:grid-cols-3 sm:grid-cols-2 gap-6"
    >
      <% posts.forEach(post => { %>
      <div
        class="bg-white rounded shadow-md p-6 mb-4 hover:shadow-lg transition-all"
      >
        <h2 class="text-2xl font-semibold text-blue-600 hover:underline">
          <a href="/posts/<%= post._id %>"><%= post.title %></a>
        </h2>
        <p class="text-gray-600 mt-2">
          <%= post.content.substring(0, 100) %>...
        </p>

        <div class="mt-4 flex justify-end space-x-2">
          <form action="/posts/<%= post._id %>/delete" method="POST">
            <button
              type="submit"
              class="bg-red-500 text-white px-4 rounded shadow hover:bg-red-600"
            >
              Delete
            </button>
          </form>
        </div>
      </div>

      <% }) %>
    </main>
  </body>
</html>
